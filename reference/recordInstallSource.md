# Record where a cached TASSEL version was installed from

Record where a cached TASSEL version was installed from

## Usage

``` r
recordInstallSource(version, source)
```

## Arguments

- version:

  TASSEL version string.

- source:

  Either `"maven"` or `"github"`.

## Value

`source`, invisibly.
