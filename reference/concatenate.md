# Concatenate phenotype tables

Concatenate (e.g. bind rows) multiple phenotype objects based on `Taxa`
column.

## Usage

``` r
concatenate(x)
```

## Arguments

- x:

  A vector of phenotype objects.
