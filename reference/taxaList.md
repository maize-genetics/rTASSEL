# Get list of taxa from TASSEL data

Returns a list of taxa from a `TasselGenotypePhenotype` object

## Usage

``` r
taxaList(tasObj)
```

## Arguments

- tasObj:

  A `TasselGenotypePhenotype` object
