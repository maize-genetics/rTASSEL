# Get dimensions of TasselDistanceMatrix object

Retrieves dimensions of a `TasselDistanceMatrix` object as a vector
(e.g. `c(10, 10)`).

## Usage

``` r
# S4 method for class 'TasselDistanceMatrix'
dim(x)
```

## Arguments

- x:

  An object of class `TasselDistanceMatrix`.
