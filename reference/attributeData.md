# Return TASSEL attribute data

Returns attribute data from an rTASSEL phenotype object

## Usage

``` r
attributeData(object, ...)

# S4 method for class 'TasselPhenotype'
attributeData(object)
```

## Arguments

- object:

  an `rTASSEL` object

- ...:

  Additional arguments, for use in specific methods
