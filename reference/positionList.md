# Get position list metadata from genotype table

Returns positional data from a `TasselGenotypePhenotype` object

## Usage

``` r
positionList(tasObj)
```

## Arguments

- tasObj:

  A `TasselGenotypePhenotype` object
