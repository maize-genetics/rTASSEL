# Record the TASSEL version that rTASSEL should load

Record the TASSEL version that rTASSEL should load

## Usage

``` r
setActiveTASSELVersion(version)
```

## Arguments

- version:

  TASSEL version string.

## Value

`version`, invisibly.
