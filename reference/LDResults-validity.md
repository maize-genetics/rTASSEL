# LDResults validation

Validates that an `LDResults` object has a properly formed `results`
data frame with the required LD columns and that `ldType` and `hetCalls`
contain allowed values.

## Arguments

- object:

  An `LDResults` object.
