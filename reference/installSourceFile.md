# Path to the file recording where a cached version came from

Path to the file recording where a cached version came from

## Usage

``` r
installSourceFile(version)
```

## Arguments

- version:

  TASSEL version string.

## Value

A character string path.
