% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tracks.R
\name{plotTracks}
\alias{plotTracks}
\title{Composite Manhattan + LD track plot}
\usage{
plotTracks(
  manhattanPlot,
  ldPlot,
  ldObj,
  zoomColor = "#db0003",
  heights = c(3, 1, 4),
  verbose = TRUE
)
}
\arguments{
\item{manhattanPlot}{A \code{ggplot} object returned by
\code{\link{plotManhattan}}.}

\item{ldPlot}{A \code{ggplot} object returned by
\code{\link{plotLD}}.}

\item{ldObj}{An object of class \code{\linkS4class{LDResults}} as
returned by \code{\link{linkageDiseq}}. The chromosome and
position range are extracted from its \code{results} slot to
determine the zoom region highlighted on the Manhattan plot.}

\item{zoomColor}{Color used for the highlight rectangle on the
Manhattan plot and the zoom connector lines/fill. Defaults to
\code{"#db0003"} (red).}

\item{heights}{A numeric vector of length 3 specifying the relative
heights of the Manhattan, zoom connector, and LD panels.
Defaults to \code{c(3, 1, 4)}.}

\item{verbose}{Logical. If \code{TRUE}, informational messages are
printed. Defaults to \code{TRUE}.}
}
\value{
A \code{patchwork} object combining the three panels.
}
\description{
Stacks a pre-built Manhattan plot and a pre-built LD
  heatmap into a single composite figure with a zoom connector track
  in between. The zoom region is automatically derived from the
  genomic coordinates in the supplied \code{\linkS4class{LDResults}}
  object and highlighted on the Manhattan panel.
}
\seealso{
\code{\link{plotManhattan}}, \code{\link{plotLD}},
  \code{\linkS4class{LDResults}}
}
