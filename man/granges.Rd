% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_tassel_genotype.R,
%   R/class_tassel_genomic_dataset.R, R/position_list_functions.R
\name{granges,TasselGenotype-method}
\alias{granges,TasselGenotype-method}
\alias{granges,TasselGenomicDataset-method}
\alias{granges,TasselGenotypePhenotype-method}
\title{Marker positions as a GRanges object}
\usage{
\S4method{granges}{TasselGenotype}(x, use.names = TRUE, use.mcols = FALSE, ...)

\S4method{granges}{TasselGenomicDataset}(x, use.names = TRUE, use.mcols = FALSE, ...)

\S4method{granges}{TasselGenotypePhenotype}(x, use.names = TRUE, use.mcols = FALSE, ...)
}
\arguments{
\item{x}{A \code{TasselGenotype}, \code{TasselGenomicDataset}, or
deprecated \code{TasselGenotypePhenotype} object.}

\item{use.names}{Name each range with its marker ID? Defaults to
\code{TRUE}.}

\item{use.mcols}{Carry the remaining \code{\link{positionList}()}
columns (\code{Site}, \code{Name}, and \code{VARIANT}) across as
metadata columns? Defaults to \code{FALSE}.}

\item{...}{Additional arguments, for use in specific methods.}
}
\value{
A \code{GRanges} object with one range per marker.
}
\description{
Returns the positions of a genotype table as a
\code{GenomicRanges::GRanges} object, which is the form the rest of
Bioconductor expects. Each marker is a width-1 range.
}
\details{
The result can be handed straight back to \code{\link{region}()} or
\code{\link{overlaps}()} to filter on, and carries the same
information as \code{\link{positionList}()} in a different shape.
}
\examples{
\dontrun{
granges(gt)

granges(gt, use.mcols = TRUE)

# Filter one genotype table on the ranges of another
gt[, sitesWhere(overlaps(granges(otherGt)))]
}

}
